
# Telegram bot token from @BotFather
TELEGRAM_TOKEN=YOUR_TELEGRAM_BOT_TOKEN_HERE

# Default interval in seconds for subscriptions (if not provided when subscribing)
DEFAULT_UPDATE_INTERVAL=300

# Database URL (Postgres recommended for production)
# Example Postgres URL for docker-compose: postgresql+psycopg://postgres:postgres@db:5432/crypto
# For local dev fallback you can use SQLite:
# sqlite:///./dev.db
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/crypto

# If you want webhook mode, set this to an HTTPS url that Telegram can reach:
# e.g. https://example.com/webhook/<BOT_TOKEN>
# If empty, bot will run in long-polling mode.
TELEGRAM_WEBHOOK_URL=

# Host/port the web app listens on (for webhook mode)
HOST=0.0.0.0
PORT=8000

# Path component for webhook (by default it's /webhook/<BOT_TOKEN>)
WEBHOOK_PATH=/webhook

# Logging level
LOG_LEVEL=info
